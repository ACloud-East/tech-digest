# 预览 `23b5ad67` — 5 点反馈修复说明

> 预览地址：**https://23b5ad67.tech-digest-74r.pages.dev**
> 生产域名 `tech-digest-74r.pages.dev` 未被触碰，可随时回滚（规则：未收到「部署生产」前只发哈希预览）。

## 针对 `21b43433` 的五点反馈

| # | 反馈 | 修复 | 验证 |
|---|------|------|------|
| 1 | 字数统计与 Word 不一致（参考原文框 5295 vs Word 4359；结构式/非结构式输出都不符） | 输出 `aiTotalChars` 与输入框计数器统一改用 `wordCountLikeWord`：**每个 CJK 字符=1，每个连续英文/数字串=1 词**，与 Word「字数」口径一致 | ✅ 真机 UI：同段含英文原文，输入框现在显示 23 字（旧口径 59 字），与 Word 口径吻合；多组用例计数正确 |
| 2 | 非结构式正文忘了去掉 `[1]` 引用编号 | 在 plain/essay 分支 prompt 追加强制指令（中英文）：严禁出现 `[1]`/`[2]`/`[?]` 等编号，且不要写「参考来源」小节 | ✅ 已写入并随函数包部署（服务端逻辑，需你自己的 API Key 在浏览器内跑一次生成确认） |
| 3 | 内容是全的，但图只拿了前大半部分，后面几张没拿到 | 根因：最终回传前端的图片硬上限 `allImages.slice(0,12)`。已将抓取上限从 12/8 提到 **30**，且 `ai-generate.js` 最终 `metaObj.images` 上限同步提到 30 | ✅ 源码已改并部署（服务端逻辑） |
| 4 | 图文要有序，像原文一样图文并茂，而不是几张图扎堆 | `injectImagesIntoContent` 改为按段落/标题**均匀分布**（封面置首段后，其余按间隔 2–4 段或每个标题后插入），避免集中 | ✅ 部署源码含 `interval` 逻辑 |
| 5 | 下载的 Word 版面巨大，因为默认是 Web 版式，想直接是页面版式 | 导出 HTML 加入 `<xml><w:WordDocument><w:View>Print</w:View></xml>` 强制 Word 以页面视图打开；保留 `@page A4` | ✅ 部署 `app.js` 已含 `<w:View>Print</w:View>` 与 `size: 210mm 297mm` |

## 版本号
- `js/app.js?v=2607291527`
- `css/style.css?v=30`
- `js/ai-generator.js?v=43`

## 说明
- 第 2、3 点为**服务端生成逻辑**，代码改动已确认进入部署包；其最终效果需你在浏览器内用自己的 API Key 实际跑一次生成来肉眼确认。第 1、4、5 点已用无头 Chromium 真机 + 部署源码静态校验双重确认。
- 若仍发现某篇文章后段配图未提取，通常是该页正文区 `<img>` 超过 30 张或图片被懒加载（data-src 未触发），可继续告诉我具体链接排查。
