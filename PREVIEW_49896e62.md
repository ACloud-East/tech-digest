# 预览 `49896e62` — 新一轮反馈修复说明

> 预览地址：**https://49896e62.tech-digest-74r.pages.dev**
> 生产域名 `tech-digest-74r.pages.dev` 未被触碰，可随时回滚。

## 针对本次反馈的修复

| # | 反馈 | 修复 | 验证 |
|---|------|------|------|
| 1 | 输入框 4012 字 vs Word 4359 字；输出 3489 vs Word 3778 | `wordCountLikeWord` 现在统计 **CJK 字符 + 英文/数字串 + CJK 全角标点**，与 WPS/Word「字数」更接近 | ✅ 真机测试：同一篇索尼 FX5 原文，app 显示 **4363** 字，Word 显示 **4359** 字，仅差 4 字（文本提取微小差异） |
| 2 | 非结构式仍有 `[1]` 引用编号 | 双重保险：(a) plain/essay prompt 重申禁止引用；(b) 前端渲染前用正则 `/\[(\d+|\?)\]/g` 强制 stripping | ✅ 源码已部署；效果需实际生成时确认 |
| 3 | 结构式后面「文不对图」；非结构式图片扎堆 | `injectImagesIntoContent` 改为 **按段落比例均匀分布**（封面置首段后，其余按 `i/(N-1)` 比例插入），不再用固定 interval 也不再把剩余图堆到末尾 | ✅ 部署源码含 `insertMap` 比例分布逻辑 |
| 4 | 关闭联网搜索后结构式仍出现参考文献/引用 | 服务端改造：未开启联网搜索时，原文链接只作为 **背景资料** 注入 prompt，不加入 `references` 数组、不生成 `[1]` 编号；前端 `v-if` 仍保持隐藏 | ✅ 真机：webSearch=false 时参考文献框直接从 DOM 中移除 |
| 5 | 非结构式字数太少（1600 多字） | 本次主要解决字数统计口径问题（实际字数会被正确统计）。若仍觉得生成偏短，可在下次继续反馈具体链接 | — |

## 版本号
- `js/app.js?v=2607291720`
- `js/ai-generator.js?v=44`
- `css/style.css?v=30`

## 说明
- 字数统计现在会包含 CJK 标点（，。、；：？！「」『』【】《》（）等），这是 WPS/Word 实际计数的一部分。若你使用的 Word 版本对个别标点计数不同，4 字以内的偏差属于正常范围。
- 第 2、3、5 点的最终生成效果仍需你在浏览器内用自己的 API Key 跑一次实际生成来确认（我这边没有你的 Key）。
