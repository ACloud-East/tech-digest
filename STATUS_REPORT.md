# 状态报告：TechDigest 数据陈旧 / 白屏 / 闪烁 修复验证

生成时间（UTC）：2026-07-12T04:48:59Z  
站点：https://acloud-east.github.io/tech-digest/

## 一、本次处理的核心矛盾

用户反馈：**"更新于1分钟前，可里面的内容，最新的居然是两小时前"**。

经排查，这一割裂由两层原因叠加：
1. **UI 标签设计误导**（代码层面）：上一版用 `manualRefreshTime`（= 用户点击那一刻）作为"新鲜度"基准，导致在 2 小时陈旧的数据上也显示"刚刚/1分钟前"，与内容真实年龄矛盾。
2. **GitHub Actions 抓取停滞**（运行层面）：cron 任务约 2–3 小时未成功推送新 `news.json`，线上数据停留在 `updateTime=02:37Z`，内容确实陈旧 2 小时。

## 二、已采取的修复

| 项 | 修复 | 提交 |
|---|---|---|
| UI 时间基准 | 移除 `manualRefreshTime`，`dataAgeText` 改回显示**真实服务器抓取时间**（"数据更新于 X 前"），与内容年龄一致 | `fab6522` |
| 绕过浏览器缓存 | `app.js` 版本号 `?v=9 → ?v=10` | `fab6522` |
| 抗瞬时失败 | workflow 抓取步骤加 3 次重试 + 放宽超时 | `fab6522` |
| 解除线上数据陈旧 | 本地跑 `npm run fetch` 生成新鲜数据（1317 篇、0 白屏），已推送 | `489ad2d` |

## 三、线上验证结果（无头 Chromium 渲染）

| 检查项 | 结果 |
|---|---|
| 线上 `index.html` 引用的 `app.js` 版本 | `?v=10` ✅ |
| 线上 `app.js` 残留 `manualRefreshTime` | 0 处（已彻底回退）✅ |
| 顶部时间文案 | **"数据更新于 3 分钟前"**（真实服务器时间）✅ |
| `{{ }}` Vue 模板闪烁残留 | 0 处 ✅ |
| DOM 中的白屏链接（`news.google.com/articles/`、`about:blank`） | 0 处 ✅ |
| 线上 `news.json` 的 `updateTime` | `2026-07-12T04:44:50Z` ✅ |
| 线上最新文章时间 | `2026-07-12T04:43:48Z` ✅ |
| 文章总数 / 主题源 | 1317 篇 / 8 个主题源齐全 ✅ |

**关键结论**：时间标签现在说的是真话——"数据更新于 3 分钟前"对应内容确实就是几分钟前抓的，之前的"1分钟前标签 vs 2小时前内容"割裂已消除。

## 四、后续运维提示

- 每小时 GitHub Actions（`0 * * * *`）会自动抓取并推送 `news.json` + `decode-cache.json`。已加 3 次重试缓解瞬时失败。
- 若再次出现"内容陈旧"，多为 Actions 运行环境问题（非代码缺陷）。可在仓库 **Actions** 标签页查看 `fetch-news` 工作流运行状态；必要时手动触发一次 run 即可立刻刷新。
- 用户侧若仍看到旧界面，请 **硬刷新（Ctrl/Cmd + F5）** 以加载 `?v=10` 的新 `app.js`。

## 五、提交记录（最近）

```
489ad2d data: 推送新鲜抓取数据(04:44:50Z, 1317篇, 0白屏) 解除Actions停滞导致的2小时陈旧
fab6522 fix: 顶部时间指示改回真实服务器抓取时间; app.js升v=10; workflow抓取加重试
a984aed fix: 修复刷新时间基准——每次加载以当前时刻显示'刚刚更新'; ?v=9
b2c8a00 fix: 添加v-cloak消除Vue模板闪烁
59a4129 fix: 主题源/死源文章解码为真实原文URL，彻底解决白屏
```
