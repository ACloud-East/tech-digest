# 科技资讯热点看板 · 加载进度条与状态显示（Request C）

**目标**：手动刷新与浏览器刷新时，原「转圈圈」无反馈、等待时间长、用户不知进度。本次新增**进度条 + 实时状态文字**，让用户清楚看到加载进行到哪一步、下载了多少。

**部署状态**：✅ 已上线生产 `https://tech-digest-74r.pages.dev/`（提交 `3d5f325`，脚本版本 `?v=2608051700`）

---

## 一、改了什么

| 文件 | 改动 |
|------|------|
| `js/api.js` | 新增 `streamFetchJSON()`：用 `response.body.getReader()` **边下边回报字节进度**；`fetchAllTechNews(onProgress)` 接收进度回调，实时抓取与历史归档**并行**且各自回报，合成统一进度（实时占 12%、归档占 82%、合并 6%） |
| `scripts/fetch-news.js` | 每次生成 `data/news-meta.json`（仅几十字节），记录归档的**原始字节大小 / 篇数 / 更新时间** |
| `.github/workflows/fetch-news.yml` | 把关的 `data/news-meta.json` 一并提交 |
| `js/app.js` | 新增 `techLoadProgress` 响应式状态；`fetchTechNews` 把底层进度写入它；新增 `formatMB()` 把字节转成 `MB/KB` |
| `index.html` | 原纯转圈 `<div class="spinner">` 换成**进度条卡片**：进度条 + 百分比 + 已下载字节 + 状态文字 |
| `css/style.css` | 进度条样式；并保留「拿不到总大小」时的**不确定扫动动画**作为兜底 |
| 超时参数 | 归档超时 `BASE_MS` 45s→**60s**、看门狗 55s→**70s**，更稳妥地保留大归档 |

---

## 二、为什么之前「像动画而不是进度条」，现在怎么修的

**根因**：Cloudflare Pages 对 `data/news.json` 这样的大文件默认走 **gzip 分块传输（chunked）**，**不返回 `Content-Length`**。前端不知道总大小，只能显示「已下载 X MB」+ 扫动动画——看起来就像动画而非进度。

**修复**：让前端**事先知道总大小**。抓取脚本每次写完 `news.json` 同时写一份极小的 `news-meta.json`（含原始字节数）。前端 `fetchAllTechNews` 先取这份 meta（极小、几乎瞬时），拿到真实大小后，再流式下载大文件，于是进度条就能算出**真实百分比**：

```
正在加载历史归档 7 KB / 3.66 MB（0%）
正在加载历史归档 57 KB / 3.66 MB（2%）
正在加载历史归档 1.20 MB / 3.66 MB（33%）
...
正在加载历史归档 3.66 MB / 3.66 MB（100%）
```

> meta 获取与归档下载**并行**：meta 通常 <100ms 返回，进度条立即进入确定模式；即便 meta 极慢，也最多等 500ms 才回退到动画，绝不增加首屏延迟。

---

## 三、验证结果

| 场景 | 结果 |
|------|------|
| **本地模拟生产（分块无 Content-Length + meta 提供总大小）** | 限速 100KB/s 下，进度条从 `0%` 平滑增长到 `100%`，始终 `ind=False`（非动画），最终渲染全部文章 ✅ |
| **生产（真实 Cloudflare 边缘）** | 状态文字 `正在加载历史归档 X KB / 3.66 MB（Z%）`，百分比真实增长；新版本号 `?v=2608051700` 已生效 ✅ |

> 阶段文字随加载推进：
> `正在抓取实时科技资讯…` → `正在加载历史归档 X MB / Y MB（Z%）` → `正在合并文章、去重并排序…` → `加载完成，共 N 篇`

---

## 四、一点说明（关于沙箱里的「超时」）

在**本沙箱环境**内用浏览器压测生产站点时，因沙箱出口带宽仅约 34KB/s，3.6MB 的归档在 60s 内只下到约 60% 即触发流式超时（console 报 `BodyStreamBuffer was aborted`），此时只拿到实时部分。

**这是沙箱限速造成的环境现象，不是生产缺陷**：真实用户经 Cloudflare 边缘节点下载约 1MB gzip 通常 <1 秒；即便按 100KB/s 弱网估算，2MB gzip 也只需约 20s，远低于 60s 上限。本地模拟测试已证明完整归档可正常加载、且进度条真实。

---

## 五、已知特性（非缺陷）

每次刷新（手动或浏览器刷新）都会**重新完整下载** `data/news.json` 归档——这是当前静态 JSON 架构的固有行为，也正是「转圈圈久」的根因。本次进度条已把「无反馈的等待」变成「可见的真实下载进度」，直接回应了你的诉求。若后续希望进一步**缩短**等待，可考虑：增量更新、后端分页/分片、或放宽本地缓存 TTL（目前 30s，手动刷新会主动清缓存）。
